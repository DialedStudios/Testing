# Homesite — Dialed Studios

Minimal, centered, Apple-like marketing site for Dialed Studios with buttery smooth transitions. Includes:
- Landing page (`index.html`)
- Pricing page (`pricing.html`)
- Newsletter page (`newsletter.html`)

## Run
Install dependencies and start the dev server:

```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```
